# Env-build notes

This file is added on a feature branch to open a **pull request** for surface capture.

- Rich markdown: [link](https://example.com), `code`, and a list.
- - External image (camo): ![img](https://httpbin.org/image/png)
 
  - ```python
    def hello():
        return "env-build"
    ```
    
